#include <iostream>
using namespace std;

float area(float r) {
    return 3.142*r*r;
}

int main() {
    float r , a;
    cout << "Enter the radius : \n";
    cin >> r;
    a = area(r);
    cout << "Area of circle is: " << a;
    return 0;
}

