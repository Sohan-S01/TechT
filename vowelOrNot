#include <stdio.h>
#include<ctype.h>

int main() {
    char c ;
    printf("Enter : \n");
    scanf("%c",&c);
    char a = tolower(c);
    if(a=='a' || a=='e' || a=='i' || a=='o' || a=='u'){
        printf("Given character is vowel");
    }else{
    printf("Not vowel");
    }
    return 0;
}
