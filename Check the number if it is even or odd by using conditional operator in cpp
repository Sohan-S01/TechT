#include <iostream>
#include <math.h>
using namespace std;

void check(int x){
    (x%2==0) ? cout<<"The number is even" : cout<<"The number is odd";
}

int main() {
    int x;
    cout<<"Enter the number :\n";
    cin >> x ;
    check(x);
    return 0;
}
